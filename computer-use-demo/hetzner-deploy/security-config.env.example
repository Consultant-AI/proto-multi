# Security Configuration for Computer Use Demo
# Copy this file to security-config.env and fill in your values

# Authentication
# Generate a strong password with: openssl rand -base64 32
DEMO_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD

# IP Whitelist (comma-separated, leave empty to allow all)
# Example: ALLOWED_IPS="1.2.3.4,5.6.7.8"
ALLOWED_IPS=""

# Your public IP (auto-detected if empty)
# Find your IP: curl ifconfig.me
MY_IP=""
